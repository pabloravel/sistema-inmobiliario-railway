{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "gunicorn api_colaborativa:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120",
    "healthcheckPath": "/salud",
    "healthcheckTimeout": 60,
    "restartPolicyType": "ON_FAILURE"
  }
} 