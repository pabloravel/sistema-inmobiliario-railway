# 🚀 CONFIGURACIÓN RAILWAY - SISTEMA INMOBILIARIO
# Copia este archivo a .env y completa las variables

# === BASE DE DATOS ===
DATABASE_URL=postgresql://usuario:password@host:puerto/database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=propiedades_db
DB_USER=pabloravel
DB_PASSWORD=tu_password

# === SEGURIDAD ===
SECRET_KEY=tu_clave_secreta_muy_segura_aqui
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# === CONFIGURACIÓN ===
PYTHONPATH=/app
PORT=8000
ENVIRONMENT=production

# === CORS (Para Railway) ===
ALLOWED_ORIGINS=*

# === WHATSAPP (Opcional) ===
WHATSAPP_API_TOKEN=tu_token_si_tienes

# === STORAGE (Para imágenes) ===
STORAGE_PATH=/app/resultados

# Variables de entorno para Railway

# Configuración del servidor
FLASK_ENV=production

# WhatsApp Business API (opcional)
WHATSAPP_TOKEN=tu_token_de_whatsapp_aqui
PHONE_NUMBER_ID=tu_phone_number_id_aqui

# JWT Secret (recomendado cambiar)
JWT_SECRET=tu_jwt_secret_super_seguro_aqui

# Base de datos (SQLite por defecto, se creará automáticamente)
DATABASE_PATH=./resultados/propiedades_estructuradas.json 